<%= form_with(model: librarian, local: true) do |form| %>
  <% if librarian.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(librarian.errors.count, "error") %> prohibited this librarian from being saved:</h2>

      <ul>
      <% librarian.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <tr>
  <td ><%= form.file_field :image, as: :file, label: false, input_html: {multiple: false} %></td>
  <td ><%= form.text_field :name, label: false %></td>
  <td ><%= form.text_field :email, label: false %></td>
  <td ><%= form.text_field :phone_no, label: false %></td>
  </tr>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
