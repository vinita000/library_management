<p id="notice"><%= notice %></p>
<p>
  <strong>Name:</strong>
  <%= @student.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @student.email %>
</p>

<p>
  <strong>Phone No:</strong>
  <%= @student.phone_no %>
</p>



<% if @student.images.attached? %>
    
   <%= image_tag url_for(student_image.variant(resize:"100x360").processed) %>
<% end %>


<%= link_to 'Edit', edit_student_path(@student) %> |
<%= link_to 'Back', students_path %>
